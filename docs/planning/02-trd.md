# TRD (기술 요구사항 정의서)

> Rust/Ratatui 기반 Claude Code 오케스트레이션 TUI 대시보드

---

## MVP 캡슐

| # | 항목 | 내용 |
|---|------|------|
| 1 | 목표 | Claude Code 오케스트레이션 진행 상황을 터미널에서 실시간 시각화 |
| 2 | 페르소나 | Claude Code 헤비 유저 (오케스트레이션/에이전트 스킬 활용 개발자) |
| 3 | 핵심 기능 | FEAT-1: Watch 모드, FEAT-2: 간트차트, FEAT-3: 에러 AI 분석 |
| 4 | 성공 지표 (노스스타) | GitHub Stars 100+ |
| 5 | 입력 지표 | 주간 다운로드 수, 이슈/PR 참여 수 |
| 6 | 비기능 요구 | 렌더링 60fps, 파일 변경 감지 < 500ms |
| 7 | Out-of-scope | Wrapper 모드, 비용 트래커, Slack 연동 |
| 8 | Top 리스크 | Rust 개발 속도 |
| 9 | 완화/실험 | 간트차트 프로토타입 먼저 구현 |
| 10 | 다음 단계 | Ratatui 프로토타입 구현 |

---

## 1. 시스템 아키텍처

### 1.1 고수준 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                      Terminal (Full Width)                        │
├────────────────────────────┬────────────────────────────────────┤
│                            │                                     │
│   TUI Dashboard (Left)     │     Claude Code Output (Right)     │
│                            │                                     │
│  ┌──────────────────────┐  │  ┌──────────────────────────────┐  │
│  │  Gantt Chart Panel   │  │  │                              │  │
│  │  (Phase/Task Tree)   │  │  │   Captured stdout/stderr     │  │
│  │                      │  │  │   from Claude Code process   │  │
│  │                      │  │  │   or PTY output              │  │
│  ├──────────────────────┤  │  │                              │  │
│  │  Detail Panel        │  │  │                              │  │
│  │  (Agent/Log/Error)   │  │  │                              │  │
│  └──────────────────────┘  │  └──────────────────────────────┘  │
└────────────────────────────┴────────────────────────────────────┘

        ▲                  ▲
        │                  │
   ┌────┴────┐       ┌────┴────┐
   │  Data   │       │  File   │
   │  Engine │       │ Watcher │
   └────┬────┘       └────┬────┘
        │                  │
   ┌────┴────┐       ┌────┴────┐
   │ TASKS.md│       │  Hook   │
   │  Parser │       │ Events  │
   └─────────┘       └─────────┘
```

### 1.2 컴포넌트 설명

| 컴포넌트 | 역할 | 왜 이 선택? |
|----------|------|-------------|
| TUI Renderer | Ratatui 기반 터미널 렌더링 엔진 | 성능 (Rust native), 바이너리 배포, 풍부한 위젯 |
| Data Engine | TASKS.md 파싱 + Hook 이벤트 병합 | 두 데이터 소스를 통합 상태 모델로 관리 |
| File Watcher | 파일 시스템 변경 감지 | notify crate로 크로스플랫폼 지원 |
| Error Analyzer | 규칙 기반 에러 패턴 매칭 | API 키 불필요, 빠른 응답, 오프라인 작동 |
| Screen Splitter | 터미널 화면 분할 관리 | cokacdir-main 패턴 참조 |

### 1.3 데이터 흐름

```
TASKS.md ──(notify watch)──▶ Parser ──▶ TaskState
                                            │
Hook Events ──(notify watch)──▶ Parser ──▶  ├──▶ Unified State Model ──▶ TUI Render
                                            │
                                     Error Analyzer
                                            │
                                     ▼ (선택적)
                                  Anthropic API
```

---

## 2. 권장 기술 스택

### 2.1 코어 런타임

| 항목 | 선택 | 이유 | 벤더 락인 리스크 |
|------|------|------|-----------------|
| 언어 | Rust (2021 edition) | 성능, 메모리 안전성, 단일 바이너리 배포 | 낮음 |
| 비동기 런타임 | Tokio | Rust 비동기 표준, 파일 I/O + 타이머 지원 | 낮음 |
| TUI 프레임워크 | Ratatui 0.28+ | 활발한 커뮤니티, 풍부한 위젯, 크로스터미널 | 낮음 |
| 터미널 백엔드 | Crossterm | 크로스플랫폼 (macOS, Linux, Windows) | 낮음 |

### 2.2 데이터 처리

| 항목 | 선택 | 이유 | 벤더 락인 리스크 |
|------|------|------|-----------------|
| TASKS.md 파서 | nom 또는 pest | Rust 네이티브 파서 컴비네이터, 유연한 문법 정의 | 낮음 |
| Hook 이벤트 파서 | serde_json | JSON 파싱 표준 | 낮음 |
| 파일 감시 | notify 6.x | 크로스플랫폼 파일 시스템 이벤트, 비동기 지원 | 낮음 |
| 설정 파일 | toml (선택) | Rust 생태계 표준 설정 포맷 | 낮음 |

### 2.3 선택적 의존성

| 항목 | 선택 | 이유 | 필수/선택 |
|------|------|------|----------|
| HTTP 클라이언트 | reqwest | Anthropic API 호출용 | 선택 (AI 분석 확장 시) |
| 로깅 | tracing | 구조화 로깅, 비동기 호환 | 권장 |
| CLI 인터페이스 | clap | 명령줄 인자 파싱 | 필수 |
| 에러 처리 | anyhow + thiserror | 에러 체이닝 + 커스텀 에러 타입 | 필수 |

### 2.4 인프라/배포

| 항목 | 선택 | 이유 |
|------|------|------|
| 패키지 배포 | crates.io + GitHub Releases | Rust 표준 배포 채널 |
| 바이너리 빌드 | cross (크로스 컴파일) | macOS/Linux/Windows 바이너리 자동 생성 |
| CI/CD | GitHub Actions | 오픈소스 표준, 무료 |
| 릴리즈 | cargo-release | 버전 관리 자동화 |

---

## 3. 비기능 요구사항

### 3.1 성능

| 항목 | 요구사항 | 측정 방법 |
|------|----------|----------|
| TUI 렌더링 | 60fps (16ms/frame) | 프레임 타이밍 측정 |
| 파일 변경 감지 | < 500ms 지연 | notify 이벤트 → 화면 반영 시간 |
| TASKS.md 파싱 | < 100ms (1000 태스크) | 벤치마크 (criterion) |
| 메모리 사용량 | < 50MB (1000 태스크 기준) | RSS 측정 |
| 바이너리 크기 | < 10MB | strip + LTO 적용 |

### 3.2 호환성

| 항목 | 요구사항 |
|------|----------|
| OS | macOS (primary), Linux, Windows (best-effort) |
| 터미널 | iTerm2, Alacritty, Terminal.app, kitty |
| Rust MSRV | 1.75+ |
| 터미널 크기 | 최소 80x24, 권장 120x40 |

### 3.3 안정성

| 항목 | 요구사항 |
|------|----------|
| 크래시 복구 | 파일 감시 실패 시 자동 재연결 |
| 파싱 에러 | 부분 파싱 실패 시 마지막 유효 상태 유지 |
| 터미널 복구 | panic 시 터미널 원래 상태 복원 (alternate screen) |

---

## 4. 핵심 데이터 모델

### 4.1 Unified State Model

```rust
// 핵심 상태 모델 (개념)
struct DashboardState {
    phases: Vec<Phase>,
    selected_task: Option<TaskId>,
    errors: Vec<ErrorEvent>,
    last_updated: Instant,
}

struct Phase {
    id: String,
    name: String,
    status: PhaseStatus,  // Pending | InProgress | Completed | Failed
    progress: f32,        // 0.0 ~ 1.0
    tasks: Vec<Task>,
    collapsed: bool,
}

struct Task {
    id: String,
    name: String,
    status: TaskStatus,   // Pending | InProgress | Completed | Failed | Blocked
    agent: Option<String>,
    started_at: Option<DateTime>,
    completed_at: Option<DateTime>,
    logs: Vec<LogEntry>,
    error: Option<ErrorInfo>,
    blocked_by: Vec<String>,
}

struct ErrorInfo {
    message: String,
    analysis: String,      // 규칙 기반 AI 분석 결과
    retryable: bool,
    occurred_at: DateTime,
}
```

### 4.2 이벤트 소스

| 소스 | 포맷 | 제공 정보 | 감시 방식 |
|------|------|----------|----------|
| TASKS.md | Markdown (커스텀) | Phase/Task 구조, 상태, 의존성 | notify file watch |
| Hook 이벤트 | JSON Lines | 에이전트 시작/종료, 에러, 도구 사용 | notify directory watch |

### 4.3 데이터 병합 규칙

| 충돌 상황 | 우선순위 | 이유 |
|----------|---------|------|
| Task 상태 불일치 | TASKS.md 우선 | 오케스트레이터가 직접 관리하는 원본 |
| 에이전트 정보 | Hook 이벤트 우선 | 실시간 에이전트 상태 제공 |
| 에러 정보 | Hook 이벤트 우선 | 상세 에러 스택트레이스 포함 |
| 타임스탬프 | Hook 이벤트 우선 | 밀리초 단위 정확한 시간 |

---

## 5. 외부 연동

### 5.1 Anthropic API (선택적)

| 항목 | 내용 |
|------|------|
| 용도 | 에러 AI 분석 (규칙 기반으로 부족할 때) |
| 필수/선택 | 선택 — API 키 없이도 규칙 기반 분석 작동 |
| 연동 방식 | reqwest + Anthropic Messages API |
| 비용 | 에러당 ~$0.01 (Haiku 모델 사용 시) |

### 5.2 Claude Code Hook

| 항목 | 내용 |
|------|------|
| 용도 | 실시간 에이전트 이벤트 수집 |
| 연동 방식 | Hook 설정 파일에 이벤트 출력 경로 지정 → 파일 감시 |
| 이벤트 타입 | tool_start, tool_end, agent_start, agent_end, error |

---

## 6. 프로젝트 구조

```
oh-my-claude-board/
├── Cargo.toml
├── src/
│   ├── main.rs              # CLI 엔트리포인트 (clap)
│   ├── app.rs               # 앱 상태 관리 + 이벤트 루프
│   ├── ui/
│   │   ├── mod.rs
│   │   ├── layout.rs        # 화면 분할 레이아웃
│   │   ├── gantt.rs         # 간트차트 위젯
│   │   ├── detail.rs        # 태스크 상세 패널
│   │   └── statusbar.rs     # 하단 상태바
│   ├── data/
│   │   ├── mod.rs
│   │   ├── tasks_parser.rs  # TASKS.md 파서
│   │   ├── hook_parser.rs   # Hook 이벤트 파서
│   │   ├── state.rs         # Unified State Model
│   │   └── watcher.rs       # 파일 감시 (notify)
│   ├── analysis/
│   │   ├── mod.rs
│   │   ├── rules.rs         # 규칙 기반 에러 분석
│   │   └── api.rs           # Anthropic API 연동 (선택)
│   └── config.rs            # 설정 (TOML)
├── tests/
│   ├── parser_test.rs
│   ├── state_test.rs
│   └── fixtures/            # 테스트용 TASKS.md, Hook 이벤트
└── docs/
    └── planning/            # 기획 문서 (이 파일들)
```

---

## 7. 테스트 전략

### 7.1 테스트 피라미드

| 레벨 | 도구 | 커버리지 목표 | 위치 |
|------|------|-------------|------|
| Unit | cargo test | >= 80% | src/ 내 #[cfg(test)] |
| Integration | cargo test (tests/) | Critical paths | tests/ |
| 수동 E2E | 실제 TASKS.md + Claude Code | Key user flows | 수동 |

### 7.2 테스트 도구

| 도구 | 용도 |
|------|------|
| cargo test | 테스트 실행 |
| cargo-tarpaulin 또는 llvm-cov | 커버리지 측정 |
| criterion | 성능 벤치마크 (파싱, 렌더링) |
| proptest | TASKS.md 파서 퍼즈 테스트 |
| insta | 스냅샷 테스트 (TUI 출력) |

### 7.3 TDD 사이클

```
🔴 RED    → 실패하는 테스트 먼저 작성 (파서, 상태 모델)
🟢 GREEN  → 테스트를 통과하는 최소한의 코드 구현
🔵 REFACTOR → 테스트 통과 유지하며 코드 개선
```

### 7.4 품질 게이트

**PR 병합 전 필수 통과:**
- [ ] cargo test 전체 통과
- [ ] cargo clippy 경고 0개
- [ ] cargo fmt 통과
- [ ] 커버리지 >= 80%
- [ ] cargo build --release 성공

---

## 8. CLI 인터페이스 설계

### 8.1 명령어 구조

```bash
# Watch 모드 (MVP 핵심)
oh-my-claude-board watch [OPTIONS]
  --tasks <PATH>     # TASKS.md 경로 (기본: ./TASKS.md)
  --hooks <PATH>     # Hook 이벤트 디렉토리 (기본: 자동 감지)
  --split <RATIO>    # 화면 분할 비율 (기본: 50:50)
  --no-ai            # AI 에러 분석 비활성화

# 버전 확인
oh-my-claude-board --version

# 설정 초기화
oh-my-claude-board init
```

### 8.2 키보드 단축키

| 키 | 동작 |
|-----|------|
| `j/k` 또는 `↑/↓` | 태스크 목록 이동 |
| `Enter` | 태스크 상세 보기 |
| `Space` | Phase 접기/펼치기 |
| `r` | 실패 태스크 재시도 |
| `q` | 종료 |
| `Tab` | 패널 간 포커스 전환 |
| `?` | 도움말 |

---

## 9. 병렬 개발 지원

### 9.1 모듈 독립성

각 모듈은 독립적으로 개발/테스트 가능하도록 설계:

| 모듈 | 의존성 | 독립 테스트 가능 |
|------|--------|----------------|
| tasks_parser | 없음 (순수 파싱) | ✅ 텍스트 입력 → 구조체 출력 |
| hook_parser | serde_json | ✅ JSON 문자열 → 이벤트 구조체 |
| state | tasks_parser, hook_parser | ✅ 파싱 결과 → 상태 모델 |
| gantt (UI) | state | ✅ 목 상태로 렌더링 테스트 |
| watcher | notify, tokio | ✅ 임시 파일로 이벤트 테스트 |
| rules (분석) | 없음 | ✅ 에러 문자열 → 분석 결과 |

---

## Decision Log

| # | 결정 | 근거 | 대안 |
|---|------|------|------|
| D1 | Rust + Ratatui | 성능, 바이너리 배포, 품질감 | Python + Textual (피벗 옵션) |
| D2 | Tokio 비동기 | 파일 감시 + TUI 이벤트 루프 통합 | async-std (생태계 작음) |
| D3 | notify crate | 크로스플랫폼 파일 감시 표준 | inotify 직접 사용 (Linux only) |
| D4 | nom 또는 pest 파서 | 유연한 TASKS.md 문법 처리 | regex (복잡한 구조 파싱 한계) |
| D5 | 규칙 기반 에러 분석 | 오프라인 작동, API 키 불필요 | LLM 전용 (비용, 의존성) |
| D6 | clap CLI | Rust 표준 CLI 라이브러리 | 직접 구현 (불필요한 재발명) |
